"use client";
import Link from "next/link";
import {useRef,useState,useEffect} from "react";
import {createPortal} from "react-dom";
import {conversao as c} from "@/content/conversao";
type Item={label:string;href:string};
export function MenuMobile({itens,telefoneHref,telefoneExibicao,contatoHref}:{itens:Item[];telefoneHref:string;telefoneExibicao:string;contatoHref:string}){const ref=useRef<HTMLDialogElement>(null);const [aberto,setAberto]=useState(false);useEffect(()=>{if(aberto){ref.current?.showModal();const anterior=document.body.style.overflow;document.body.style.overflow="hidden";return()=>{document.body.style.overflow=anterior}}},[aberto]);function fechar(){ref.current?.close();setAberto(false)}return <div className="md:hidden"><button type="button" onClick={()=>setAberto(true)} aria-expanded={aberto} aria-label={c.menuAbrir} className="flex h-11 w-11 items-center justify-center text-navy-900"><svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true"><path d="M3 6h18M3 12h18M3 18h18"/></svg></button>{aberto&&createPortal(<dialog ref={ref} className="rf-mobile-dialog" onClose={()=>setAberto(false)} onClick={e=>{if(e.target===e.currentTarget)fechar()}} aria-label={c.servicos}><div className="mobile-dialog-inner"><button type="button" onClick={fechar} className="mobile-close" aria-label={c.menuFechar}>×</button><nav>{itens.map(i=><Link key={i.href} href={i.href} onClick={fechar}>{i.label}</Link>)}</nav><Link href={contatoHref} className="rf-button" onClick={fechar}>{c.orcamento}</Link><a href={telefoneHref} className="rf-text-link">{telefoneExibicao}</a></div></dialog>,document.body)}</div>}
