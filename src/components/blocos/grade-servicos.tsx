import Image from "next/image";
import Link from "next/link";
import { servicos } from "@/content/servicos";
import { conversao as c, destaquesServico } from "@/content/conversao";
import { Seta } from "@/components/ui/primitivos";
export function GradeServicos({tom="tela"}:{tom?:"tela"|"claro"}){return <section className={`rf-section ${tom==="tela"?"rf-surface":""}`} id="servicos"><div className="container-rf"><div className="section-heading"><div><p className="rf-eyebrow">{c.solucoes.etiqueta}</p><h2>{c.solucoes.titulo}</h2></div><p>{c.solucoes.texto}</p></div><div className="service-grid">{servicos.slice(0,3).map(s=><Link className="service-card" key={s.slug} href={`/servicos/${s.slug}`}><div className="service-image"><Image src={s.imagem.src} alt={s.imagem.alt} fill sizes="(min-width:1024px) 360px, (min-width:640px) 45vw, 90vw" className="object-cover"/></div><div className="service-body"><h3>{s.nomeCurto}</h3><p>{s.resumo}</p><ul>{destaquesServico[s.slug].resumo.slice(0,2).map(r=><li key={r}>✓ {r}</li>)}</ul><span>{c.detalhes}<Seta/></span></div></Link>)}</div><div className="secondary-services">{servicos.slice(3).map(s=><Link href={`/servicos/${s.slug}`} key={s.slug}><div><h3>{s.nomeCurto}</h3><p>{s.resumo}</p></div><Seta/></Link>)}</div></div></section>}
